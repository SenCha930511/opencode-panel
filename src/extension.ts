import type * as vscode from "vscode";

// Stub entry: real activation (server lifecycle, webview view providers,
// command registrations) lands in later todos (8, 10, 19-22). Keeping this
// minimal on purpose so the build pipeline compiles on its own.
export function activate(_context: vscode.ExtensionContext): void {}

export function deactivate(): void {}
